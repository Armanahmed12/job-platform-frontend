import React from 'react';

const JobDetails = ({jobData}) => {
    console.log(jobData)
    return (
        <div>
              JobDetails
        </div>
        
    );
};

export default JobDetails;